describe('GetDecisionByIdController', () => {
  describe('Success case', () => {
    it('returns a decision when given a valid id', () => {
      // GIVEN
      // WHEN
      // THEN
    })
  })

  describe('Error cases', () => {
    it('throws a 404 error if the id does not exist', () => {
      // GIVEN
      // WHEN
      // THEN
    })

    it('throws a 404 error if the id is not found', () => {
      // GIVEN
      // WHEN
      // THEN
    })

    it('throws a 401 error if the apiKey is not valid', () => {
      // GIVEN
      // WHEN
      // THEN
    })

    it('throws a 401 error if the apiKey is not present', () => {
      // GIVEN
      // WHEN
      // THEN
    })
  })
})
